node_modules/
dist/
build/
coverage/
.env
.env.*
!.env.example
*.log
.DS_Store
# GraphQL codegen output IS committed so Docker/CI builds are hermetic (no codegen step
# needed, and the build context no longer depends on files that only exist locally).
cypress/videos/
cypress/screenshots/
.vite/
