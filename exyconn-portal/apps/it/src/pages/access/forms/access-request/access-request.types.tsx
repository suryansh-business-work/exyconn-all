import type { ItAccessRequestFieldsFragment } from '@exyconn/shell/graphql/generated';

/** One access request (or password reset), as the generated fragment types it. */
export type AccessRequestRow = ItAccessRequestFieldsFragment;
