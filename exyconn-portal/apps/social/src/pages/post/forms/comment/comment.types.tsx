import type { SocialCommentFieldsFragment } from '@exyconn/shell/graphql/generated';

/** A comment as the thread renders it, straight off the generated fragment. */
export type SocialCommentRow = SocialCommentFieldsFragment;
