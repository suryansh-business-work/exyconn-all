# Per-IP request rate for portal-server (GraphQL API): 30 r/s sustained, bursts of 60.
# The zone is defined in http/00-exyconn-http-context.conf. Installed to /etc/nginx/snippets/.
limit_req zone=exyconn_portal_api burst=60 nodelay;
limit_req_status 429;
