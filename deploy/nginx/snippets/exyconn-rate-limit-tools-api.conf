# Per-IP request rate for the public tools API: 10 r/s sustained, bursts of 20.
# The zone is defined in http/00-exyconn-http-context.conf. Installed to /etc/nginx/snippets/.
limit_req zone=exyconn_tools_api burst=20 nodelay;
limit_req_status 429;
