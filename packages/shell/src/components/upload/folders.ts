/**
 * CDN namespaces the portal uploads into. They live here rather than in each screen
 * because a folder name is only useful if every surface that uploads the same kind of
 * file agrees on it — support attachments come from the console, the employee's own
 * ticket form and their replies, and all three must land in the same place.
 */
export const SUPPORT_UPLOAD_FOLDER = 'support';
