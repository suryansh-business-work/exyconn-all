query ListEmailConfigs {
  listEmailConfigs {
    id
    label
    host
    port
    secure
    username
    password
    fromAddress
    isActive
  }
}

mutation CreateEmailConfig($input: EmailConfigInput!) {
  createEmailConfig(input: $input) {
    id
  }
}

mutation UpdateEmailConfig($id: ID!, $input: EmailConfigInput!) {
  updateEmailConfig(id: $id, input: $input) {
    id
  }
}

mutation DeleteEmailConfig($id: ID!) {
  deleteEmailConfig(id: $id)
}

query ListImageConfigs {
  listImageConfigs {
    id
    label
    provider
    publicKey
    privateKey
    urlEndpoint
    isActive
  }
}

mutation CreateImageConfig($input: ImageConfigInput!) {
  createImageConfig(input: $input) {
    id
  }
}

mutation UpdateImageConfig($id: ID!, $input: ImageConfigInput!) {
  updateImageConfig(id: $id, input: $input) {
    id
  }
}

mutation DeleteImageConfig($id: ID!) {
  deleteImageConfig(id: $id)
}

mutation SendTestEmail($id: ID!, $to: String!) {
  sendTestEmail(id: $id, to: $to)
}

mutation TestImageUpload($id: ID!, $file: String!, $fileName: String!) {
  testImageUpload(id: $id, file: $file, fileName: $fileName)
}

query ListSlackConfigs {
  listSlackConfigs {
    id
    label
    botToken
    defaultChannel
    isActive
  }
}

mutation CreateSlackConfig($input: SlackConfigInput!) {
  createSlackConfig(input: $input) {
    id
  }
}

mutation UpdateSlackConfig($id: ID!, $input: SlackConfigInput!) {
  updateSlackConfig(id: $id, input: $input) {
    id
  }
}

mutation DeleteSlackConfig($id: ID!) {
  deleteSlackConfig(id: $id)
}

mutation SendTestSlackMessage($id: ID!, $channel: String!) {
  sendTestSlackMessage(id: $id, channel: $channel)
}

query ListGithubConfigs {
  listGithubConfigs {
    id
    label
    owner
    repo
    token
    isActive
  }
}

mutation CreateGithubConfig($input: GithubConfigInput!) {
  createGithubConfig(input: $input) {
    id
  }
}

mutation UpdateGithubConfig($id: ID!, $input: GithubConfigInput!) {
  updateGithubConfig(id: $id, input: $input) {
    id
  }
}

mutation DeleteGithubConfig($id: ID!) {
  deleteGithubConfig(id: $id)
}

mutation TestGithubConnection($id: ID!) {
  testGithubConnection(id: $id)
}

query ListSlackChannels {
  listSlackChannels {
    id
    name
    isPrivate
    isMember
  }
}

query ListTrackerBuilds {
  listTrackerBuilds {
    id
    status
    conclusion
    branch
    url
    startedAt
  }
}

query TrackerBuildSettings {
  trackerBuildSettings {
    slackChannels
  }
}

mutation StartTrackerBuild($platforms: [TrackerPlatform!]!, $ref: String!) {
  startTrackerBuild(platforms: $platforms, ref: $ref)
}

mutation SaveTrackerBuildSettings($slackChannels: [String!]!) {
  saveTrackerBuildSettings(slackChannels: $slackChannels) {
    slackChannels
  }
}
