{
  "name": "@exyconn/regex",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "The validation patterns every form checks against, with ZERO dependencies. One definition of an email, a URL, a phone number, a slug or a GSTIN, so the portals, the website and the server cannot drift apart on what they accept.",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "format": "prettier --write \"src/**/*.ts\"",
    "test": "vitest run --passWithNoTests"
  },
  "prettier": "@exyconn/config/prettier.json",
  "devDependencies": {
    "@exyconn/config": "workspace:*",
    "@testing-library/jest-dom": "^7.0.1",
    "eslint": "^10.9.1",
    "prettier": "^3.4.2",
    "typescript": "^6.0.3",
    "vitest": "^5.0.0"
  }
}
