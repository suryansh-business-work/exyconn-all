---
import Page from "../../layouts/Page.astro";
import { getBlogPost, getBlogPosts, formatDate } from "../../lib/portal";

const { slug } = Astro.params;
const post = slug ? await getBlogPost(slug) : null;

if (!post) {
  return new Response(null, { status: 404 });
}

// Get related posts (same tags, excluding current)
const allPosts = await getBlogPosts();
const relatedPosts = allPosts
  .filter((p) => p.slug !== post.slug && p.tags.some((tag) => post.tags.includes(tag)))
  .slice(0, 3);

const SITE_URL = (Astro.site?.toString() || "https://exyconn.com/").replace(/\/$/, "");
const postUrl = `${SITE_URL}/blog/${post.slug}`;
const coverAbs = /^https?:\/\//.test(post.coverImage)
  ? post.coverImage
  : `${SITE_URL}${post.coverImage}`;

const pageJsonLd = [
  {
    "@context": "https://schema.org",
    "@type": "BlogPosting",
    headline: post.title,
    description: post.summary,
    image: coverAbs,
    datePublished: post.publishedAt,
    dateModified: post.publishedAt,
    author: {
      "@type": "Organization",
      name: "Exyconn",
      url: SITE_URL,
    },
    publisher: {
      "@type": "Organization",
      name: "Exyconn",
      logo: { "@type": "ImageObject", url: `${SITE_URL}/favicon.svg` },
    },
    mainEntityOfPage: { "@type": "WebPage", "@id": postUrl },
    keywords: post.tags.join(", "),
  },
  {
    "@context": "https://schema.org",
    "@type": "BreadcrumbList",
    itemListElement: [
      { "@type": "ListItem", position: 1, name: "Home", item: `${SITE_URL}/` },
      { "@type": "ListItem", position: 2, name: "Blog", item: `${SITE_URL}/blog` },
      { "@type": "ListItem", position: 3, name: post.title, item: postUrl },
    ],
  },
];
---

<Page
  metaTitle={post.title + " | Exyconn Blog"}
  metaDescription={post.summary}
  metaKeywords={post.tags.join(", ")}
  metaImage={post.coverImage}
  ogType="article"
  jsonLd={pageJsonLd}
>
  <main class="bg-gradient-to-b from-gray-50 to-white min-h-screen">
    <!-- Hero Section -->
    <section class="relative pt-12 pb-8 px-6">
      <div class="max-w-4xl mx-auto">
        <!-- Breadcrumb -->
        <nav class="flex items-center gap-2 text-sm text-gray-500 mb-8">
          <a href="/" class="hover:text-blue-600 transition-colors">Home</a>
          <i class="fa-solid fa-chevron-right text-xs"></i>
          <a href="/blog" class="hover:text-blue-600 transition-colors">Blog</a>
          <i class="fa-solid fa-chevron-right text-xs"></i>
          <span class="text-gray-900 font-medium truncate max-w-[200px]">{post.title}</span>
        </nav>

        <!-- Tags -->
        <div class="flex flex-wrap gap-2 mb-4">
          {
            post.tags.map((tag) => (
              <span class="text-xs font-medium text-blue-600 bg-blue-50 px-3 py-1 rounded-full">
                {tag}
              </span>
            ))
          }
        </div>

        <!-- Title -->
        <h1 class="text-4xl md:text-5xl font-bold text-gray-900 mb-6 leading-tight">
          {post.title}
        </h1>

        <!-- Summary -->
        <p class="text-xl text-gray-600 mb-8 leading-relaxed">
          {post.summary}
        </p>

        <!-- Meta Info (Date & Read Time only) -->
        <div
          class="flex flex-wrap items-center gap-4 pb-8 border-b border-gray-200 text-sm text-gray-500"
        >
          <span class="flex items-center gap-1.5">
            <i class="fa-regular fa-calendar"></i>
            {formatDate(post.publishedAt)}
          </span>
          <span class="flex items-center gap-1.5">
            <i class="fa-regular fa-clock"></i>
            {post.readTime}
          </span>
        </div>
      </div>
    </section>

    <!-- Cover Image -->
    <section class="px-6 mb-12">
      <div class="max-w-5xl mx-auto">
        <img
          src={post.coverImage}
          alt={post.title}
          class="w-full h-[400px] object-cover rounded-2xl shadow-lg"
        />
      </div>
    </section>

    <!-- Content -->
    <section class="px-6 pb-16">
      <div class="max-w-3xl mx-auto">
        <article
          class="prose prose-lg max-w-none prose-headings:text-gray-900 prose-headings:font-bold prose-p:text-gray-700 prose-a:text-blue-600 prose-strong:text-gray-900 prose-blockquote:border-blue-500 prose-blockquote:bg-blue-50 prose-blockquote:py-1 prose-blockquote:px-4 prose-blockquote:rounded-r-lg prose-li:text-gray-700"
          set:html={post.content}
        />

        <!-- Share Section -->
        <div class="mt-12 pt-8 border-t border-gray-200">
          <div class="flex flex-wrap items-center justify-between gap-4">
            <span class="text-gray-600 font-medium">Share this article:</span>
            <div class="flex items-center gap-3">
              <a
                href={`https://twitter.com/intent/tweet?text=${encodeURIComponent(post.title)}&url=${encodeURIComponent("https://exyconn.com/blog/" + post.slug)}`}
                target="_blank"
                rel="noopener"
                class="w-10 h-10 rounded-full bg-gray-100 flex items-center justify-center text-gray-600 hover:bg-blue-500 hover:text-white transition-colors"
              >
                <i class="fa-brands fa-x-twitter"></i>
              </a>
              <a
                href={`https://www.linkedin.com/sharing/share-offsite/?url=${encodeURIComponent("https://exyconn.com/blog/" + post.slug)}`}
                target="_blank"
                rel="noopener"
                class="w-10 h-10 rounded-full bg-gray-100 flex items-center justify-center text-gray-600 hover:bg-blue-700 hover:text-white transition-colors"
              >
                <i class="fa-brands fa-linkedin-in"></i>
              </a>
              <button
                onclick="navigator.clipboard.writeText(window.location.href)"
                class="w-10 h-10 rounded-full bg-gray-100 flex items-center justify-center text-gray-600 hover:bg-gray-900 hover:text-white transition-colors"
                title="Copy link"
              >
                <i class="fa-solid fa-link"></i>
              </button>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Related Articles -->
    {
      relatedPosts.length > 0 && (
        <section class="px-6 py-16 bg-gray-50">
          <div class="max-w-6xl mx-auto">
            <h2 class="text-2xl font-bold text-gray-900 mb-8">Related Articles</h2>
            <div class="grid md:grid-cols-3 gap-8">
              {relatedPosts.map((related) => (
                <a
                  href={`/blog/${related.slug}`}
                  class="group bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden hover:shadow-lg transition-all"
                >
                  <div class="h-40 overflow-hidden">
                    <img
                      src={related.coverImage}
                      alt={related.title}
                      class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
                    />
                  </div>
                  <div class="p-5">
                    <div class="flex gap-2 mb-2">
                      {related.tags.slice(0, 2).map((tag) => (
                        <span class="text-xs text-blue-600 bg-blue-50 px-2 py-0.5 rounded-full">
                          {tag}
                        </span>
                      ))}
                    </div>
                    <h3 class="font-bold text-gray-900 group-hover:text-blue-600 transition-colors line-clamp-2">
                      {related.title}
                    </h3>
                    <p class="text-sm text-gray-500 mt-2">{formatDate(related.publishedAt)}</p>
                  </div>
                </a>
              ))}
            </div>
          </div>
        </section>
      )
    }

    <!-- CTA Section -->
    <section class="px-6 py-16 bg-gradient-to-br from-blue-600 to-purple-600">
      <div class="max-w-3xl mx-auto text-center">
        <h2 class="text-3xl font-bold text-white mb-4">Ready to Transform Your Business?</h2>
        <p class="text-blue-100 text-lg mb-8">
          Let's discuss how Exyconn can help you leverage AI and automation for growth.
        </p>
        <a
          href="/get-a-quote"
          class="inline-flex items-center gap-2 px-8 py-4 bg-white text-blue-600 font-semibold rounded-full hover:shadow-xl transition-all"
        >
          Get Started
          <i class="fa-solid fa-arrow-right"></i>
        </a>
      </div>
    </section>
  </main>
</Page>

<style>
  .line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  .prose .lead {
    font-size: 1.25rem;
    line-height: 1.75rem;
    color: #4b5563;
  }
</style>
