---
interface FAQItem {
  question: string;
  answer: string;
}

interface Props {
  title?: string;
  subtitle?: string;
  faqs: FAQItem[];
}

const {
  title = "Frequently Asked Questions",
  subtitle = "Everything you need to know about our services",
  faqs,
} = Astro.props;
---

<section class="py-20 lg:py-28 bg-gradient-to-b from-white via-gray-50/50 to-white">
  <div class="max-w-4xl mx-auto px-6 lg:px-8">
    <!-- Section Header -->
    <div class="text-center mb-14">
      <span
        class="inline-flex items-center gap-2 bg-blue-100 text-blue-700 text-sm font-semibold px-4 py-2 rounded-full mb-6"
      >
        <i class="fa-solid fa-circle-question"></i>
        FAQ
      </span>
      <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold text-gray-900 mb-4">
        {title}
      </h2>
      <p class="text-lg text-gray-600">{subtitle}</p>
    </div>

    <!-- FAQ Accordion -->
    <div class="space-y-4">
      {
        faqs.map((faq, index) => (
          <details
            class="group bg-white rounded-2xl border border-gray-100 shadow-sm hover:shadow-md transition-all duration-300"
            open={index === 0}
          >
            <summary class="flex items-center justify-between gap-4 p-6 cursor-pointer list-none">
              <div class="flex items-center gap-4">
                <span class="flex items-center justify-center w-10 h-10 bg-gradient-to-br from-blue-500 to-purple-600 text-white text-sm font-bold rounded-xl shrink-0">
                  {String(index + 1).padStart(2, "0")}
                </span>
                <h3 class="text-lg font-semibold text-gray-900 group-open:text-blue-600 transition-colors">
                  {faq.question}
                </h3>
              </div>
              <div class="shrink-0 w-8 h-8 bg-gray-100 group-open:bg-blue-100 rounded-full flex items-center justify-center transition-colors">
                <i class="fa-solid fa-chevron-down text-gray-500 group-open:text-blue-600 group-open:rotate-180 transition-all duration-300 text-sm" />
              </div>
            </summary>
            <div class="px-6 pb-6 pt-2 ml-14">
              <p class="text-gray-600 leading-relaxed">{faq.answer}</p>
            </div>
          </details>
        ))
      }
    </div>

    <!-- Bottom CTA -->
    <div
      class="mt-14 text-center p-8 bg-gradient-to-br from-blue-50 to-purple-50 rounded-3xl border border-blue-100"
    >
      <div
        class="inline-flex items-center justify-center w-14 h-14 bg-gradient-to-br from-blue-500 to-purple-600 rounded-2xl mb-4"
      >
        <i class="fa-solid fa-headset text-white text-xl"></i>
      </div>
      <h3 class="text-xl font-bold text-gray-900 mb-2">Still have questions?</h3>
      <p class="text-gray-600 mb-6">
        Can't find the answer you're looking for? Our team is here to help.
      </p>
      <a
        href="/contact"
        class="inline-flex items-center gap-2 bg-gray-900 text-white font-semibold px-6 py-3 rounded-full hover:bg-gray-800 transition-all"
      >
        <i class="fa-solid fa-message"></i>
        Contact Support
      </a>
    </div>
  </div>
</section>

<style>
  details summary::-webkit-details-marker {
    display: none;
  }
</style>
