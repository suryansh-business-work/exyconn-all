// The design system's shape tokens, by path (its entry imports MUI). The tracker's product
// decision carries over to the phone: nothing is rounder than TRACKER_RADIUS.
export { TRACKER_RADIUS, borderWidth } from '@exyconn/ui/src/tokens/border.token';
