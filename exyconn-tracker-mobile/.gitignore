# Continuous native generation: `expo prebuild` writes these in CI on every build.
/android/
/ios/
.expo/
# The upload keystore is decoded from a repository secret in CI and never committed.
*.jks
*.keystore
